spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/asviridova/asviridova
          search-paths: {application}
        encrypt:
          enabled: false
  security:
    user:
      name: root
      password: root

server:
  port: 5000

encrypt:
  key: shop-app-secret

